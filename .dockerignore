# VCS
.git
.gitignore

# Local env / secrets — never into the image
.env
certs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Docs / CI / deploy (not needed at runtime)
docs/
*.md
.github/
workflow-example.yaml
